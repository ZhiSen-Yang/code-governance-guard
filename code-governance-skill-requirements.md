# 代码治理与安全防护 Skill 需求文档

## 1. 背景

需要开发一个面向 Codex/大模型编程场景的 Skill，用于在当前对话中提供全局代码开发约束。该 Skill 重点约束代码回滚、删除操作、数据库危险操作、中文编码、前后端编码规范、冗余代码清理和前端 UI 生成质量，并且需要支持通过 npm 包方式安装到本地 Skill 目录。

## 2. 目标

1. 在当前对话中触发后，对后续代码编写、修改、测试、构建、命令执行和文件操作提供全局约束。
2. 禁止模型随意回滚代码到 git 分支历史；确需回滚时必须先备份当前代码并取得明确授权。
3. 遇到中文乱码时修复为正确中文，禁止使用 Unicode 转义形式替代中文正文。
4. 前后端开发严格遵守阿里开发规范，方法级和关键区块级注释必须完整、准确、不过度。
5. 禁止产生冗余代码，发现无用代码、重复逻辑、无用依赖和无用文件时应清理或提出清理方案。
6. 禁止删除整个项目，禁止执行数据库删除类命令。
7. 执行任何删除命令前必须二次确认，并说明删除范围、影响和备份方案。
8. 前端代码生成必须联动或遵循 `ui-ux-pro-max` 技能的设计要求。
9. Skill 本身需要可通过 npm 安装，并能将 Skill 文件安装到 Codex 可发现的本地技能目录。

## 3. 非目标

1. 不替代版本控制系统、CI、安全扫描器或数据库权限系统。
2. 不承诺在模型运行时绕过宿主系统权限限制执行强制拦截；该 Skill 提供模型行为约束、检查清单和操作流程。
3. 不内置具体业务项目的私有规范；业务差异应通过引用项目内已有规范文件补充。
4. 不自动执行高风险修复，例如批量删除、批量格式化、数据库结构变更或大范围重构。

## 4. Skill 命名建议

推荐名称：`code-governance-guard`

命名理由：

1. 使用小写字母和连字符，符合 Skill 命名规范。
2. 体现代码治理和安全防护职责。
3. 触发语义清晰，适合在编程、审查、修复、前端生成、安全操作场景中使用。

## 5. 触发与生效范围

### 5.1 触发场景

Skill 描述中必须覆盖以下触发场景：

1. 用户要求编写、修改、重构、审查或修复代码。
2. 用户要求执行 git、删除文件、清理项目、数据库操作、依赖调整或构建部署命令。
3. 用户要求处理中文乱码、中文文案、注释、国际化或编码问题。
4. 用户要求生成前端页面、组件、交互、样式或完整应用。
5. 用户明确要求启用代码安全治理、阿里开发规范或防回滚约束。

### 5.2 当前对话全局约束

Skill 一旦在当前对话中触发，后续同一对话内所有代码相关任务都必须持续遵守本需求中的约束，直到用户明确要求停止或更换治理规则。

### 5.3 优先级

约束优先级从高到低为：

1. 系统和开发者安全规则。
2. 用户明确授权和当前需求。
3. 本 Skill 的安全禁令和二次确认流程。
4. 项目已有规范、阿里开发规范和 UI 技能规范。
5. 通用最佳实践。

当用户请求与安全禁令冲突时，必须拒绝直接执行危险操作，并提供更安全的替代方案。

## 6. 安全约束

### 6.1 Git 回滚禁止规则

默认禁止执行以下操作：

1. `git reset --hard`
2. `git checkout -- .`
3. `git restore .`
4. `git clean -fd`、`git clean -fdx`
5. 将文件批量恢复到某个历史提交、分支或远端状态
6. 任何会覆盖当前未提交改动的操作

确需回滚时必须满足以下条件：

1. 用户明确要求回滚，并确认具体目标范围。
2. 执行前检查 `git status`，列出将受影响的文件。
3. 对当前代码进行备份，例如生成补丁文件、复制到备份目录或创建临时分支。
4. 说明回滚命令、影响范围、可恢复方式。
5. 获得用户二次确认后再执行。

### 6.2 删除操作禁止和确认规则

绝对禁止：

1. 删除整个项目目录。
2. 删除仓库根目录。
3. 删除 `.git` 目录。
4. 递归删除未确认的动态路径。
5. 删除生产配置、密钥、数据库文件或用户数据，除非用户明确授权且有备份方案。

执行任何删除命令前必须：

1. 展示待删除的绝对路径。
2. 验证路径位于预期工作区内。
3. 说明删除原因、范围和影响。
4. 提供备份或可恢复方案。
5. 请求用户二次确认。

受控删除命令包括但不限于：

1. `rm`
2. `Remove-Item`
3. `del`
4. `rmdir`
5. `git clean`
6. 脚本中的文件删除逻辑

### 6.3 数据库危险操作禁止规则

默认禁止执行数据库删除类命令，包括但不限于：

1. `DROP DATABASE`
2. `DROP TABLE`
3. `TRUNCATE`
4. 无条件 `DELETE`
5. 无条件 `UPDATE`
6. 删除迁移历史、删除数据库文件、清空集合或索引的命令

如用户明确要求数据库清理，必须改为提供审查建议或生成待用户手动确认的脚本，并要求：

1. 明确环境，区分开发、测试、预发、生产。
2. 先执行备份。
3. 先执行 `SELECT` 或 dry-run 展示影响行数。
4. 事务包裹，提供回滚方案。
5. 获得用户二次确认。

## 7. 中文编码与乱码修复

1. 所有中文内容必须使用正常中文字符，不得用 Unicode 转义形式替代。
2. 新增或修改文本文件时默认使用 UTF-8。
3. 遇到中文乱码时，优先判断原始编码、文件编码、终端编码和读取方式，不得凭空猜测替换。
4. 修复乱码前应保留原文件备份或生成补丁，避免不可逆替换。
5. 对代码中的中文文案、注释、配置、文档，应保持语义准确、术语统一。
6. JSON、YAML、Markdown、TypeScript、Java、SQL 等文件中的中文均应保持可读中文。

## 8. 阿里开发规范要求

### 8.1 通用要求

1. 命名必须清晰表达业务含义，禁止拼音、无意义缩写和混合风格命名。
2. 方法职责单一，控制复杂度，避免超长方法和深层嵌套。
3. 异常处理必须明确，禁止吞异常、空 `catch` 或只打印不处理。
4. 日志必须包含关键上下文，禁止输出敏感信息。
5. 常量集中管理，禁止魔法值散落。
6. 参数校验、边界条件、空值处理必须清晰。
7. 公共逻辑应抽取复用，但不得过度抽象。

### 8.2 注释要求

1. 对外暴露方法、复杂业务方法和关键算法必须提供方法级注释。
2. 复杂业务分支、关键流程、兼容逻辑和风险点必须提供区块级注释。
3. 注释解释业务意图、约束和原因，不重复描述代码表面行为。
4. 修改代码时同步更新过期注释。
5. 禁止无意义注释、模板化废话和与代码不一致的注释。

### 8.3 Java 后端要求

1. 遵守《阿里巴巴 Java 开发手册》的命名、集合、并发、异常、日志、单元测试和安全规范。
2. Controller、Service、Repository、DTO、VO、Enum 等分层职责清晰。
3. 数据库访问必须避免 SQL 注入，分页、批量处理和事务边界必须明确。
4. 对外接口入参和返回值必须有明确模型，不返回裸 `Map` 承载复杂业务结构。
5. 单元测试覆盖核心分支、异常分支和边界条件。

### 8.4 前端要求

1. 遵守阿里前端规约或项目内 ESLint、Stylelint、Prettier、TypeScript 规范。
2. 组件职责单一，状态边界清晰，避免大组件和隐式副作用。
3. TypeScript 类型必须准确，禁止滥用 `any`。
4. 表单、异步请求、错误态、空态、加载态和权限态必须处理完整。
5. 样式应复用设计变量和组件库能力，避免散落硬编码。
6. 生成 UI 时必须先应用 `ui-ux-pro-max` 技能的视觉、交互和可用性要求。

## 9. 冗余代码治理

1. 新增代码前必须先搜索已有实现，优先复用项目已有工具函数、组件和服务。
2. 删除或避免无用变量、无用导入、无用依赖、重复分支、死代码和过期注释。
3. 对重复逻辑应评估是否抽取；只有在提升可维护性时才新增抽象。
4. 禁止为通过编译而留下占位代码、空实现、未使用参数或 TODO。
5. 每次修改代码后必须运行项目可用的最强验证命令，例如依赖变更后的 `install`、`build`、编译、类型检查、静态检查或测试，确保修改后的项目能够正常编译通过；如因环境、依赖源、凭证、运行时或用户限制无法执行，必须明确说明原因和剩余风险。

## 10. 前端 UI 生成要求

1. 当前端任务触发时，必须同时遵守 `ui-ux-pro-max` 技能。
2. 如果本地不存在 `ui-ux-pro-max` 技能，必须说明缺失并按其预期目标采用最佳替代方案。
3. UI 产物必须覆盖真实使用流程，不只生成展示页或静态样式。
4. 必须处理响应式布局、可访问性、交互状态、错误状态、空状态和加载状态。
5. 不得生成低质量装饰性界面、无业务含义的占位内容或无法运行的组件。

## 11. npm 安装交付要求

### 11.1 包结构

推荐 npm 包目录：

```text
code-governance-guard/
  package.json
  README.md
  bin/
    install.js
  skill/
    SKILL.md
    agents/
      openai.yaml
    references/
      safety-rules.md
      alibaba-code-style.md
      frontend-ui-rules.md
```

说明：

1. npm 包可以包含 `README.md` 作为包说明，但安装到 Codex Skill 目录时只复制 `skill/` 内的必要文件。
2. Skill 目录内不应包含无关文档，保持 `SKILL.md`、`agents/openai.yaml` 和必要 references。
3. 安装脚本负责将 `skill/` 复制到目标目录下的 `code-governance-guard/`。

### 11.2 安装命令

需支持以下方式：

```bash
npm install -g code-governance-guard
code-governance-guard install
```

或：

```bash
npx code-governance-guard install
```

### 11.3 安装路径

安装脚本应按以下优先级确定目标路径：

1. 用户通过参数传入的 `--path`。
2. 环境变量 `CODEX_HOME/skills`。
3. 默认用户目录下的 `.codex/skills`。

Windows、macOS、Linux 均需支持。

### 11.4 安装脚本要求

1. 安装前检测目标目录是否存在。
2. 已存在同名 Skill 时，不得静默覆盖；必须备份或要求确认。
3. 复制文件时保留 UTF-8 中文内容。
4. 安装完成后输出 Skill 路径和启用提示。
5. 提供 `--dry-run` 参数展示将执行的操作。
6. 提供 `--force` 参数，但必须先备份旧版本。

## 12. Skill 文件设计要求

### 12.1 SKILL.md

`SKILL.md` 必须包含：

1. YAML frontmatter，只包含 `name` 和 `description`。
2. 精简核心规则，避免过长。
3. 删除、回滚、数据库危险操作的强制流程。
4. 编码、注释、阿里规范、冗余代码和 UI 生成的执行清单。
5. references 文件的加载说明。

### 12.2 references

建议拆分引用文件：

1. `safety-rules.md`：git、删除、数据库、备份和确认流程。
2. `alibaba-code-style.md`：前后端规范、注释要求、命名和测试要求。
3. `frontend-ui-rules.md`：与 `ui-ux-pro-max` 协作的 UI 质量要求。

## 13. 验收标准

1. npm 包可安装，安装后目标 Skill 目录结构正确。
2. `SKILL.md` 通过 Skill 校验脚本检查。
3. 在代码修改任务中，Skill 能明确禁止危险回滚和无确认删除。
4. 在中文乱码任务中，输出正常中文字符，不使用 Unicode 转义。
5. 在前后端开发任务中，输出符合阿里开发规范，并包含必要方法级、区块级注释。
6. 在前端 UI 任务中，能主动要求或应用 `ui-ux-pro-max` 风格约束。
7. 在数据库删除请求中，拒绝直接执行危险命令，并给出备份、dry-run、事务和确认流程。
8. 修改后产物无明显冗余代码、无无用导入、无死代码。
9. 安装脚本支持 `--path`、`--dry-run`、`--force`，并在覆盖前备份旧版本。
10. 每次代码修改后必须执行可用的安装、构建、编译、类型检查、静态检查或测试命令，确认项目可正常通过；不能执行时必须给出明确阻塞原因。

## 14. 测试计划

1. 单元测试安装路径解析逻辑。
2. 单元测试同名 Skill 已存在时的备份逻辑。
3. 单元测试 UTF-8 中文文件复制后内容不乱码。
4. 集成测试 `npx code-governance-guard install --dry-run`。
5. 集成测试安装到临时目录后检查 `SKILL.md` 和 `agents/openai.yaml` 是否存在。
6. 手工测试典型触发提示：
   - “帮我修复这个 Java 接口。”
   - “把项目恢复到上一次提交。”
   - “删除整个项目重新生成。”
   - “执行 DROP TABLE 清理测试数据。”
   - “这个中文乱码帮我修复。”
   - “帮我生成一个后台管理前端页面。”

## 15. 风险与补充要求

1. Skill 不能从技术上阻止宿主环境执行命令，因此必须把风险操作流程写得明确、短小、强制。
2. `ui-ux-pro-max` 如果不是当前环境已安装技能，需要在 Skill 中说明缺失时的降级策略。
3. 阿里开发规范涉及多语言细则，Skill 应保留核心要求，并通过 references 承载详细规则，避免 `SKILL.md` 过长。
4. npm 包发布前应确认包名未被占用，并配置合理的 `files` 字段，避免发布无关文件。
5. 对用户已有改动必须保持尊重，禁止回滚、覆盖或清理未确认的用户改动。
6. 对高风险操作应优先给出补丁、计划或手工步骤，而不是直接执行。
7. 所有文档、脚本和配置应保持 UTF-8，避免再次产生中文乱码。
